
<div class="containter">
<div class="row">
<div class="col-6">
   <h2>Fresh Burgers</h2>

<ul>
  {{#each burgers}}
  {{#unless devoured}}
 <li>
{{burger_name}}</li><button class="devour-it btn btn-primary" data-id="{{id}}" data-newDevoured="{{devour}}">Devour it</button>
  {{/unless}}
{{/each}}
</ul>
</div>

  <div class="col-6">
   <h2>Devoured Burgers</h2>
   <ul class="devoured">
  {{#each burgers}}
  {{#if devoured}}
 <li>
{{burger_name}}</li>
{{!-- <button class="devour-it" data-id="{{id}}" data-newDevoured="{{devour}}">Devour it</button> --}}
  {{/if}}
{{/each}}
</ul>
   
  </div>
</div>

</div>
{{!-- add a burger container --}}
<div class="container">
      <div class="row">
        <div class="col-3"></div>
          <div class="col-6">
            <h2>Add a Burger</h2>
          </div>
        <div class="col-3"></div>
      </div>


  <div class="row">
    <div class="col-3"></div>
        <div class="col-6">
              <form class="create-form">
            <div  class="form-group">
                  <label for="newBurger">Burger Name:</label>
                  <input type="text" id="newBurger"  name="burger">
            </div>
                  <button class="btn btn-primary type="submit">Add Burger</button>
              </form>
          <div class="col-3"></div>    
        </div>
  </div>
</div>





















